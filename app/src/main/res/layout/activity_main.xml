<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_black"
    android:padding="24dp">

    <TextView
        android:id="@+id/glitch_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:letterSpacing="0.2"
        android:lineSpacingExtra="4dp"
        android:text="X3NC1\nAPK\nANALYZER"
        android:textColor="@color/neon_green"
        android:textSize="40sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="48dp" />

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/neon_purple"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/glitch_title"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/tv_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:text="&gt; Static APK Analysis Tool\n&gt; Permissions // IP // API Keys // URLs"
        android:textColor="@color/neon_purple"
        android:textSize="13sp"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/divider"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/btn_scan"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="[ SELECT APK TO SCAN ]"
        android:fontFamily="monospace"
        android:textColor="@color/bg_black"
        android:textSize="14sp"
        android:letterSpacing="0.1"
        android:backgroundTint="@color/neon_green"
        android:layout_marginTop="48dp"
        app:layout_constraintTop_toBottomOf="@id/tv_subtitle"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/btn_developer"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="[ DEVELOPER ]"
        android:fontFamily="monospace"
        android:textColor="@color/neon_purple"
        android:textSize="13sp"
        android:backgroundTint="@color/bg_card"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/btn_scan"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:text="&gt; v1.0 "
        android:textColor="#444444"
        android:textSize="11sp"
        android:gravity="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginBottom="16dp" />

</androidx.constraintlayout.widget.ConstraintLayout>